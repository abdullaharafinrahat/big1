const crypto=require('crypto'),{distanceKm}=require('../shared/distance.util');const items=[{id:'donor-1',name:'Sample Donor',phone:'01700000001',bloodGroup:'O+',district:'Chattogram',area:'Panchlaish',latitude:22.36,longitude:91.83,available:true,reliabilityScore:.95}];exports.list=async(q={})=>items.filter(x=>(!q.bloodGroup||x.bloodGroup===q.bloodGroup)&&(!q.district||x.district.toLowerCase()===q.district.toLowerCase()));exports.create=async b=>{const x={id:crypto.randomUUID(),available:true,reliabilityScore:.5,...b,createdAt:new Date().toISOString()};items.push(x);return x};exports.getById=async id=>{const x=items.find(i=>i.id===id);if(!x)throw Object.assign(Error('Donor not found'),{status:404});return x};exports.nearby=async(q={})=>items.map(x=>({...x,distanceKm:distanceKm(q.latitude,q.longitude,x.latitude,x.longitude)})).filter(x=>x.available&&x.distanceKm<=Number(q.radiusKm||25)).sort((a,b)=>a.distanceKm-b.distanceKm);exports._all=items;