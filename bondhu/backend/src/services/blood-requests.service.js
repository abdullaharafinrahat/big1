const crypto=require('crypto'),mod=require('./moderation.service');const items=[];exports.list=async(q={})=>items.filter(x=>!q.status||x.status===q.status);exports.create=async b=>{const x={id:crypto.randomUUID(),status:'pending_moderation',urgency:'normal',...b,createdAt:new Date().toISOString()};items.push(x);mod.enqueue('blood-request',x);return x};exports.getById=async id=>{const x=items.find(i=>i.id===id);if(!x)throw Object.assign(Error('Blood request not found'),{status:404});return x};exports._all=items;