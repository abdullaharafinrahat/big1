const q=[];exports.enqueue=(type,item)=>{q.push({type,id:item.id,item,status:'pending',createdAt:new Date().toISOString()});return item};exports.queue=()=>q.filter(x=>x.status==='pending');exports.queueSize=()=>exports.queue().length;function set(type,id,status,user){const x=q.find(i=>i.type===type&&i.id===id);if(!x)throw Object.assign(Error('Moderation item not found'),{status:404});x.status=status;x.reviewedBy=user?.sub||user?.id;x.reviewedAt=new Date().toISOString();if(x.item)x.item.status=status;return x}exports.approve=(t,i,u)=>set(t,i,'approved',u);exports.reject=(t,i,u)=>set(t,i,'rejected',u);