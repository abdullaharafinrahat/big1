const r=require('express').Router(),c=require('../controllers/admin.controller'),{authenticate}=require('../middleware/auth.middleware'),{requireRole}=require('../middleware/roles.middleware');r.use(authenticate,requireRole('admin','moderator'));r.get('/dashboard',c.dashboard);r.get('/moderation',c.moderationQueue);r.post('/moderation/:type/:id/approve',c.approve);r.post('/moderation/:type/:id/reject',c.reject);module.exports=r;
