import{getAccessToken}from'../auth/session.js';export const API_BASE_URL=localStorage.getItem('bondhu.apiBaseUrl')||'/api';export async function apiRequest(path,opt={}){const h=new Headers(opt.headers||{});if(opt.body&&!(opt.body instanceof FormData)&&!h.has('Content-Type'))h.set('Content-Type','application/json');const t=getAccessToken();if(t)h.set('Authorization',`Bearer ${t}`);const r=await fetch(`${API_BASE_URL}${path}`,{...opt,headers:h});const ct=r.headers.get('content-type')||'';const d=ct.includes('json')?await r.json():await r.text();if(!r.ok){throw Object.assign(new Error(d?.message||r.statusText),{status:r.status,data:d})}return d}export const toJsonBody=p=>JSON.stringify(p);
