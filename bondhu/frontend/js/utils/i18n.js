const KEY='bondhu.lang';let dict={};export const getLanguage=()=>localStorage.getItem(KEY)||document.documentElement.lang||'bn';export function setLanguage(l){localStorage.setItem(KEY,l);document.documentElement.lang=l}export async function translatePage(base='./'){try{dict=await fetch(`${base}locales/${getLanguage()}.json`).then(r=>r.json())}catch{dict={}}document.querySelectorAll('[data-i18n]').forEach(e=>e.textContent=e.dataset.i18n.split('.').reduce((o,k)=>o?.[k],dict)||e.textContent)}export const t=(k,f=k)=>k.split('.').reduce((o,x)=>o?.[x],dict)||f;
