import{createDonor}from'../api/donors.api.js';import{showToast}from'../components/toast.js';import{formToObject,validatePhone}from'../utils/validators.js';const f=document.querySelector('#donor-register-form');f?.addEventListener('submit',async e=>{e.preventDefault();const d=formToObject(f);d.available=!!f.available?.checked;if(!validatePhone(d.phone))return showToast('Invalid phone','error');try{await createDonor(d);showToast('Donor registered');f.reset()}catch(err){showToast(err.message,'error')}});
