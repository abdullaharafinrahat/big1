(() => { const A=window.BondhuApp,$=A.$; A.setupShell('dashboard'); const acc=A.getAccount(), profile=A.getProfile(), status=A.getStatus(), progress=A.profileCompletion(profile); $('#welcome-title').textContent = `${A.lang()==='bn'?'স্বাগতম':'Welcome'}, ${profile?.fullName || acc.name}`; $('#profile-progress-text').textContent = `${progress}% ${A.lang()==='bn'?'সম্পূর্ণ':'Complete'}`; $('#profile-progress-bar').style.width=`${progress}%`; $('#stat-status').textContent=status==='verified'?'Verified':status==='pending'?'Pending':status==='revision'?'Correction':'Incomplete'; $('#profile-status-list').innerHTML=`<li class="ok">✓ Account Verified</li><li class="${progress>=100?'ok':'warn'}">${progress>=100?'✓':'●'} Profile ${progress}% Complete</li><li class="${profile?.presentDistrict||profile?.city?'ok':'warn'}">${profile?.presentDistrict||profile?.city?'✓':'●'} Address ${profile?.presentDistrict||profile?.city?'Added':'Missing'}</li><li class="${status==='verified'?'ok':'warn'}">${status==='verified'?'✓':'●'} Verification ${status}</li>`; })();
