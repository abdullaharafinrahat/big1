import{listHospitals}from'../api/hospitals.api.js';import{showToast}from'../components/toast.js';const list=document.querySelector('#hospital-list'),form=document.querySelector('#hospital-search');const render=a=>list.innerHTML=(a||[]).map(h=>`<article class="card"><strong>${h.name}</strong><p>${h.address||h.district||''}</p><p>${h.phone||''}</p></article>`).join('')||'<p>No hospitals found.</p>';async function load(p={}){try{render(await listHospitals(p))}catch(e){showToast(e.message,'error');render([])}}form?.addEventListener('submit',e=>{e.preventDefault();load(Object.fromEntries(new FormData(form)))});load();
