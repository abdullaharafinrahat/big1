export function initHeroCarousel(root=document){const c=root.querySelector('[data-carousel]');if(!c)return;const s=[...c.querySelectorAll('.hero-slide')],d=c.querySelector('.carousel-dots');let i=0;const show=n=>{i=n;s.forEach((x,k)=>x.classList.toggle('is-active',k===i));d?.querySelectorAll('button').forEach((b,k)=>b.setAttribute('aria-current',String(k===i)))};s.forEach((_,k)=>{const b=document.createElement('button');b.type='button';b.onclick=()=>show(k);d?.append(b)});show(0);setInterval(()=>show((i+1)%s.length),6500)}
